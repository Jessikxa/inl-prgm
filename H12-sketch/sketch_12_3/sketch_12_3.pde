/*
  if keypressed --- teken rectangle -> op plek waar de muis is ingedrukt
 --fill(coloourrsss);
 of maakt int's for r,g,b;
 
 voor de rectangle heb je mouseX en mouseY nodig zodat het print op de juiste plek|^
 */
//12.3   :OOO
value = 0;
int RM = random(255);
fill(225, 0+r, 128)
  void setup() {
  size(300, 300);
}
void draw() {
  fill(colours);
  rect(mouseX, mouseY, 50, 50);
}
void mousePressed() {
  if (mousePressed()) {
    //draw rectangle at location en fill different colour every time
  }
}
